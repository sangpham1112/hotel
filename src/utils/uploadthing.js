import { generateComponents } from "@uploadthing/react";

// Assuming OurFileRouter is defined somewhere else in your JavaScript code.
export const { UploadButton, UploadDropzone, Uploader } = generateComponents();
